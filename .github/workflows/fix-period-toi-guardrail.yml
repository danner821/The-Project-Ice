name: Fix Period TOI Guardrail

on:
  push:
    paths:
      - '.github/workflows/fix-period-toi-guardrail.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply period TOI guardrail
        run: python .github/scripts/fix_period_toi_guardrail.py
      - name: Validate world.js
        run: node --check artifacts/project-ice/public/world.js
      - name: Commit gameplay fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add artifacts/project-ice/public/world.js
          if git diff --cached --quiet; then
            echo "No gameplay changes to commit"
            exit 0
          fi
          git commit -m "Add period-level TOI deployment guardrail"
          git push
