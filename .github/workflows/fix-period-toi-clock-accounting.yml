name: Fix Period TOI Clock Accounting

on:
  push:
    paths:
      - '.github/workflows/fix-period-toi-clock-accounting.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fix period TOI clock accounting
        run: python .github/scripts/fix_period_toi_clock_accounting.py
      - name: Validate world.js
        run: node --check artifacts/project-ice/public/world.js
      - name: Commit gameplay fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add artifacts/project-ice/public/world.js
          if git diff --cached --quiet; then
            echo "No gameplay changes to commit"
            exit 0
          fi
          git commit -m "Fix period TOI clock accounting"
          git push
