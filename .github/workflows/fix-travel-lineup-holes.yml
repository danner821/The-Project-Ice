name: Fix Travel Lineup Holes
on:
  push:
    paths:
      - '.github/scripts/fix_travel_lineup_holes.py'
      - '.github/workflows/fix-travel-lineup-holes.yml'
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python .github/scripts/fix_travel_lineup_holes.py
      - run: node --check artifacts/project-ice/public/travel-hockey-roster-world.js
      - run: |
          git config user.name 'Project Ice Bot'
          git config user.email 'actions@github.com'
          git add artifacts/project-ice/public/travel-hockey-roster-world.js
          git commit -m 'Fill every Travel lineup position naturally' || exit 0
          git pull --rebase origin main
          git push origin main
