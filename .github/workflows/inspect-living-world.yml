name: Inspect Living World
on:
  push:
    paths:
      - '.github/workflows/inspect-living-world.yml'
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/inspect_living_world.py
