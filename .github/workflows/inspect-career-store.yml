name: Inspect Career Store
on:
  push:
    paths:
      - '.github/workflows/inspect-career-store.yml'
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/inspect_career_store.py
