name: Inspect Phase 1 round two
on:
  push:
    paths:
      - '.github/workflows/inspect-phase1-round2.yml'
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/inspect_phase1_round2.py
