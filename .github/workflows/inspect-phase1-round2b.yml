name: Inspect Phase 1 round two b
on:
  push:
    paths:
      - '.github/workflows/inspect-phase1-round2b.yml'
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/inspect_phase1_round2b.py
