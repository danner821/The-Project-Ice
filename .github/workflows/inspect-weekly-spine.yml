name: Inspect Weekly Spine
on:
  push:
    paths:
      - '.github/workflows/inspect-weekly-spine.yml'
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/inspect_weekly_spine.py
