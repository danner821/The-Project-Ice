# Project Ice — Dynamic Potential 2.0

Status: **design locked; disposable iPhone restore/read-back/cleanup verified; live migration still gated by shadow evaluation and controlled recovery.**
Baseline: career backup exported at game date 2025-09-04 (Junior 2025–26 preseason). **Never commit the backup to GitHub.**

## Core model (approved decisions across initial choices and three questionnaire rounds)

- One **dynamic actual potential rating**. No separately predetermined, immutable hidden ability ceiling. Visible role is derived consistently from the numerical rating; actual OVR can exceed the current projection.
- **Scouting certainty** measures confidence in the *current projection*, not how good the player is. Sustained unexplained outperformance or underperformance lowers certainty before a potential change. A formal potential change resets certainty to **Medium**, regardless of direction. Subsequent consistent evidence and scout exposure can rebuild it toward High.
- Five visible trends: Rapidly Falling, Falling, Stable, Rising, Rapidly Rising. Strength and persistence of evidence determine the trend. Rapid trends indicate unusually strong sustained evidence and a more imminent possible reevaluation, **never a guaranteed change**.
- Evidence: production and impact adjusted for age, position (including a separate goalie model), competition strength, usage, ice time, underlying available game statistics and comparable players, plus demonstrated attribute growth and form. Never invent missing historical data.
- Performance **primarily determines potential movement**. Low certainty allows a reevaluation but does not cause an automatic upgrade/downgrade. Exceptional performances can justify reevaluation despite low scouting exposure; ordinary cases require sustained evidence. Avoid rewarding one anomalous game.
- Most flexibility before 23; increasingly stable near ages 27–28; rare increases after 27. A truly dominant 20-year-old could reach Franchise; an equally dominant 30-year-old can remain Elite while exceeding the projected OVR. Potential declines only after prolonged context-adjusted underperformance, accounting for age and opportunity.
- Higher potential generally accelerates youth development. Growth becomes progressively more expensive as attributes/OVR approach their projected level, following tier- and personality-dependent curves. **Sustained breakout evidence gradually relaxes the potential-related XP penalty ahead of official promotion**; promotion makes the new prices effective immediately. Existing earned XP, spent XP, attribute balances and upgrades remain unchanged.
- Keep the hard **85 OVR high-school cap**. Physical decline precedes other regression as players age; potential label stays established in the thirties. No injury simulation or fatigue.
- Hidden development DNA/personality affects XP generation, distribution, performance sensitivity, early/late growth, usage response and long-term development. Personality has **no direct potential-reevaluation bonus**; it may affect results only indirectly via actual development/performance. Never reveal personality labels on player-facing profiles.
- Generation targets, complete draft class: roughly **5–12 Elite per year**, with meaningful variation in class strength, and **2–3 Franchise per decade** as a soft long-run target. Exceptional organic breakouts may exceed these targets. These are game calibration targets, not claims about EA's proprietary formulas.

## Canonical sources and compatibility

- Numerical potential and its role, confidence, accuracy, signal and trend must have **one authoritative owner** in the canonical player's development record, with synchronized legacy player-facing fields. Do not allow the Home, Player and Scouting tabs to use inconsistent precedence.
- Weekly canonical evaluator is the only in-season source of potential changes. Existing legacy annual potential mutation is unreachable and must not be revived. Seasonal historical analysis may produce **proposals**, not duplicate live mutations.
- All current high-school and development statistics, attributes, XP, history, awards, and completed/active season status must survive unchanged unless a specific potential migration is reviewed and authorized.
- Scope of **retrospective audit**: generated high-school players (including the career player). Real-world prospects outside HS, and factual real-world players currently inserted into HS rosters, must not be rerated retroactively; if their future in-season rules change, review separately.
- The existing external-prospect source must remain untouched by the migration. Where the saved world stores the career player in more than one place, update all mirrors **atomically** after an audited migration.

## Verified backup baseline and audit caveats

- Export format: projectice-career-backup v1; active record id must equal career:<activeCareerId> and careerId must equal activeCareerId.
- Backup date: 2025-09-04, current season hs-2025-2026, phase preseason; eight teams and 160 roster slots, comprising 148 generated HS players (including career player) and 12 real HS roster prospects; 191 external prospects.
- Generated players with two archived HS histories: 148/148; with at least 10 games in both archived seasons: 141/148.
- **Known canonical mismatch:** career player's top-level potential **74 / Medium / Rising**, but nested development potential **68 / High / Stable**. Do not silently pick a field and rewrite the save. Preserve both in the original backup. A read-only evidence audit found the career player's season production increased from 7 to 23 points over two 28-game seasons, with OVR 70→72. A candidate rating must be calibrated against position, deployment, competition and historical context before acceptance. **108 generated players have stale development.currentAge=14 despite roster age 15–18**; retrospective scoring must use archived season ages and current roster age, not development.currentAge.
- Static backup envelope validation and Node structured-serialization round-trip passed. On September 24, 2026, the user confirmed **PASS — isolated IndexedDB restore, read-back, and temporary database cleanup verified on iPhone**, 2025-09-04, 160 roster players, 191 external prospects. This verifies a disposable restore, not yet a production restore or complete app boot from imported data. Keep the original JSON backup.

## September 4 backup: preliminary shadow-evidence checkpoint

- Isolated iPhone restore/read-back/disposal has now passed. This still does not validate a full gameplay boot from imported data.
- Independent read-only local scoring across 148 generated HS players: 11 strong breakout-review flags, 10 additional strong-recent-production flags, 7 persistent low-relative-production flags, 7 insufficient-comparable-evidence flags, 113 ordinary.
- Historical scoring used age recorded inside each season archive rather than stale nested development.currentAge. Same-group (F/D/G), age-near comparables; skater points per 60 minutes (>=10 GP), goalie save percentage (>=100 shots and 3 GP). These are saved-HS-world relative ranks, **not** NHL talent ratings or actual potential migrations.
- Alacai: root 74 / nested 68 (unchanged); freshman 7 points / 28 GP, sophomore 23 points / 28 GP; age-/position-relative first-season 9.2 percentile and latest-season 95.7 percentile. Strong-breakout-review evidence, **no assigned new potential** pending the calibrated full evaluator.
- All external and HS-rostered real prospect ratings remain excluded. Original backup SHA-256 397aefb65e24db4b0d36bea6d2b6481898c4f2ae78c5c25f215fa2fc19f2f25f.

## Second-stage read-only shadow evaluator (2026-09-24)

- GitHub tools: `artifacts/project-ice/tools/potential-v2-shadow.js` (explicit, offline Node-only; **not loaded by the live game**) and `potential-v2-shadow.test.js` (12 passing synthetic checks).
- Reads a user-exported JSON backup and writes a **separate report JSON only**. Preserves the source file, omits realPlayer/persistentProspect identities, excludes unfinished/future season archives, and never proposes a numeric rating. Deterministic, idempotent, no live browser DB mutations.
- Uses complete high-school peer-group percentile measures: age-near position cohorts, points/60 (skaters) or save % (goalies) with supporting available contextual stats and recorded OVR change. Requires sample thresholds and records data gaps.
- The independent local Python cross-check of the 2025-09-04 backup found **5 strong-breakout-review**, **5 additional breakout-review**, **10 decline-evidence-review**, **8 persistent-underperformance-review**, **7 missing-history** and **113 retained pending calibration**, totaling 148 generated HS players. These differ from the first screen's 11 breakout flags because the second pass additionally weighs shot production and plus/minus and uses stricter evidence thresholds. This Python run mirrors the JS formulas; it is **not yet a direct Node run of the GitHub script on this 56 MB backup**.
- Alacai: prior 9.2nd percentile, latest 95.7th percentile on main position/age peer production, 91.9th percentile on the contextual blend, +2 OVR, provisional **Rapidly Rising evidence**. Root 74 and nested development 68 both remain unchanged. His precise potential/uncertainty is held for longitudinal tier calibration and explicit user review.
- Next gate: position/level/potential-relative performance expectations, larger synthetic age/deployment/goalie and long-career scenarios, a clear before/after preview, and a real recovery mechanism before enabling any production migration.

## Potential-tier-calibrated shadow analysis (2026-09-24)

- New offline tool: `tools/potential-v2-calibration.js` and `tools/potential-v2-calibration.test.js` (15 passing synthetic checks). Unlike generic position/age relative percentiles, this pass weights age-near position-matched peers by proximity to each player's **current potential hypothesis**. It excludes the subject from its reference pool and reports effective sample size, expected and observed performance, performance/expectation ratio, and tier-relative percentile.
- This tool is NOT imported by any live-game script and NEVER writes to IndexedDB or the source backup. It emits a separate JSON report. It uses the same `2025-09-04` backup baseline; no live save state was changed. Historical archives that finish after the game date are ignored. It neither exposes hidden personality nor assigns proposed numerical potential.
- Actual 56 MB snapshot processed offline: 148 generated HS players; 12 real HS-rostered and 191 external real prospects excluded. 138 have calibrated evidence without conflicting fields, 1 has a potential-field conflict requiring review, 7 have insufficient two-season history, and 2 lack age/position peers.
- **Career-player comparison against both competing saved potential hypotheses:** Alacai's sophomore production **2.3878 points per 60 minutes**; against potential 74 he has 45 eligible comparables (39.9 effective), expected 1.3967 P/60, **1.71×** expected and 95.8th tier-relative percentile; against potential 68 he has 23 comparables (20.1 effective), expected 1.3499 P/60, **1.77×** expected and 96.6th percentile. This supports challenging the old projection but **does not establish a new numerical potential**. Retain both 74 and 68 in the untouched original until a canonical migration is explicitly approved.
- Critical limitation: past potential at each archived game date is not consistently recorded. Current saved potential is a comparison hypothesis, never presented as a historically verified tier. Likewise, HS-world percentile is not an NHL-equivalent forecast. Small goalie peer pools are withheld when the reference sample is too small.
- Next: calibrate against position-specific league/competition levels when post-HS data exists, add XP pricing shadow simulation and age/prime fixtures, then build a production restore with explicit confirm and checksum / newer-save checks before any live mutation.

## XP pricing shadow checkpoint (2026-09-24)

- The **offline-only** files `tools/potential-v2-xp-shadow.js` and `tools/potential-v2-xp-shadow.test.js` mirror the current live rating/repeated-upgrade/potential/age cost, then model a potential- and personality-dependent OVR proximity surcharge.
- Exceptional young breakout evidence, once independently verified by the potential evaluator, **gradually eliminates the additional proximity surcharge**. At full teenage breakout evidence, an outdated Bottom-6/AHL tier cannot impose *more* than its existing baseline XP price. A formal potential upgrade immediately changes the base potential multiplier and proximity curve, providing further price relief. Evidence is not inferred from scouting confidence, so High certainty never automatically discounts XP.
- For ages 28–32 and 33+, the extra proximity-charge relief is much smaller; aging and legacy rating cost continue to slow development. The current hard HS OVR cap of 85 is still enforced only by the existing gameplay upgrade system; the offline report flags its need and does not grant upgrades.
- A saved root/development potential conflict yields **two separate price scenarios**, not a silently chosen winner. Existing attributeXP, ratings and upgrade counts are read without mutation. The pricing simulator is NOT loaded by `world.js`; actual weekly evaluator/XP integration is still gated.
- Pure-function regression tests passed for baseline compatibility, higher-tier price benefits, rising proximity costs, verified teenage breakout protection, midcareer/aging differences, hidden personality price variation, goalie attributes, root/development conflicts, incomplete attributes, and no XP spending.
- Independent Python mirror run against the original September 4 JSON confirmed the career record remained unchanged. Alacai (RW, age 16, 72 OVR) wristShotPower 78 and 62 saved XP: **at root potential 74**, existing price 99, shadow no-evidence price 114, hypothetical strong (.8) breakout price 100; **at nested potential 68**, existing price 105, shadow no-evidence price 158, hypothetical .8 breakout price 107; **after hypothetical promotion to 84**, price 88. These are **illustrative scenario prices**, not deployed upgrade costs, and .8 is not claimed to be measured current weekly evidence.
- This exposed and corrected a first-draft shadow formula that over-penalized already-outperforming young players even with strong evidence. The updated formula has a dedicated regression test preventing that failure.

## Integrated evidence → XP and recovery preflight checkpoint

- New **offline-only, zero-write** integration tool: `tools/potential-v2-evidence-xp.js`, with 22 passing synthetic assertions. It joins two independent archived-season scoring paths with current-root and nested-potential tier hypotheses, generates provisional breakout-evidence strengths **only when both paths and sample/peer thresholds agree**, and passes those strengths to the existing XP shadow simulator. It does not migrate a potential rating or modify XP. Missing records or weak tier-peer samples result in **zero bonus**. Scouting certainty is not an XP-discount input.
- Explicit Alacai potential disagreement (root 74, nested 68) remains **two independent scenarios**, never an automatic winner. The evaluator does not use a null potential as a fallback rating. Real HS-rostered and external prospects are excluded.
- Independent Python mirror on the user's immutable September 4 backup (source SHA-256 `397aefb65e24db4b0d36bea6d2b6481898c4f2ae78c5c25f215fa2fc19f2f25f`) analyzed 148 generated HS players; excluded 12 real HS-rostered and 191 external prospects. There were **149 potential-specific pricing scenarios**: 6 strong provisional breakouts, 5 provisional, 124 no validated two-season breakout, and 14 withheld because evidence was insufficient. The source backup hash was identical before and after this independent run. This **was not a direct Node execution of the GitHub integration source against the 56MB backup**, so user-facing quantitative results must retain that qualification.
- Alacai's independently mirrored root-74 scenario produced provisional evidence `0.846`, and the nested-68 scenario `0.891`. In both cases the strong historical breakout eliminated the **additional** new proximity surcharge, but normal XP costs still apply: wrist-shot power 78, saved 62 XP, costs 99 (74 case) versus 105 (68 case); *no attribute upgrades or save mutations*.
- A separate `tools/potential-v2-restore-guard.js` and test suite added **15 passing synthetic preflight checks**. This is read-only: validates backup envelope, same active career/player identities, exact career record key, current/backup revisions and dates, and flags newer live progress. **It is not a production restore**, and no write permission is exposed. When the live career has newer progress, the guard instructs taking a fresh verified backup instead of proceeding.
- The isolated iPhone restore/read-back/cleanup was verified previously. A production restore with rollback, explicit user confirmation, and a full game boot validation **remains a strict gate** before any live mass potential migration.
- Near-term engineering: test the actual combined Node script on an exported fixture/snapshot without committing private save data, calibrate weekly evidence/uncertainty for future live simulation, then build user-confirmed recovery and migration in that order.

## Offline weekly evaluator and longitudinal simulation checkpoint

- New **offline-only** `tools/potential-v2-weekly-shadow.js`, a weekly pure-function simulator that consumes same-competition-tier, same-position, age-near and potential-near peer statistics. It rejects cross-league contamination; **the only high-school team level is Varsity**, including legacy HS records with no team-level label. AA and AAA are separate **travel** tiers, not additional HS tiers. With too few eligible comparables, inadequate player sample, no new games, missing league identity or the career's unresolved root/nested potential conflict, it **withholds the evaluation** instead of guessing.
- The weekly model uses skater points per 60 or goalie save percentage with adequate samples; documented attribute growth is a supporting factor. It does not use hidden development personality as a potential-reevaluation modifier, does not penalize imaginary injuries and does not increment confidence without new observations. Sustained disagreement reduces confidence before potential changes; trends include Rapidly Rising and Rapidly Falling, and any actual modeled tier move resets certainty to **Medium (55)**.
- Idempotent by season/week, deterministic hash reevaluation, no hidden RNG, no player/save mutation. The output is a **proposal only** and is not yet imported by `world.js`. Separate suites cover one-week evaluation, same/divergent competition cohorts, goalies, no-observation certainty, conflicting potential, 36-week breakout timelines, Franchise eligibility and prolonged Elite underperformance.
- **Franchise safeguard** discovered during a 52-week stress run: without a multi-season gate, a synthetic 17-year-old could advance from Elite to Franchise after one spectacular NHL season. The revised offline model now requires an already-Elite 19–27-year-old NHL player with **two distinct recorded dominant NHL seasons**, each at least 60 GP, 90 points and 1.2 PPG, plus a 24-week cooldown and an additional low-probability reevaluation roll. Age-30 Elite performance and an isolated 20-game scoring burst cannot trigger Franchise promotion.
- An Elite player's potential also cannot be downgraded merely from one weak season; the offline model requires at least two documented poor seasons (minimum 20 GP and performance/expected under 0.72 each), followed by sustained current-season negative evidence. Full age/level population calibration and real archived NHL data remain future work.
- Re-ran **all seven offline test suites** from the latest GitHub `main` files: all PASS. These are synthetic unit/regression and deterministic longitudinal tests, **not** an actual Cloudflare/iPhone game simulation or a full production-restore test.
- **Legacy live-engine risks identified, not yet changed:** the original weekly evaluator uses general OVR rather than tier-specific peer expectations; processes a mixed NPC/external prospect pool; and enforces `minimumPotential = overall + 2` for young players, potentially forcing a potential increase without performance evidence. Do not activate the new engine or historical migration until these ownership and real-prospect exclusions are safely integrated and tested.
- Next implementation gate: build read-only in-game shadow comparison against the canonical weekly evaluation without writing either result, validate simulation timing and real-player exclusions on an isolated save, and complete a genuine user-confirmed recovery path before proposing live player migrations.

## Varsity-only correction and offline legacy/V2 comparison (2026-09-24)

- User clarified the current high-school career has **Varsity only**. The offline weekly evaluator now normalizes all HS players to Varsity, including legacy records with no explicit level, and keeps AA versus AAA travel hockey separate. There is **no JV team, JV simulation, or JV development pathway** in the current game. Added regression tests for both rules.
- New developer-only `tools/potential-v2-live-comparison.js` and corresponding tests compare the **mathematical evidence entry** of the legacy live evaluator with the pure V2 weekly shadow, while explicitly filtering real HS-rostered and external prospects and refusing the unresolved Alacai 74/68 potential mismatch. It is not imported by gameplay and does not execute or mutate the actual stateful live evaluator.
- This comparison confirms a legacy implementation concern: the live formula applies an implicit minimum potential of current OVR + 2 for players through age 23. For Alacai, 72 OVR plus 2 equals 74 even when the nested saved potential says 68. The V2 model **withholds** a conflicted evaluation rather than applying an automatic upgrade.
- New and existing synthetic offline regression suites must pass before any future Cloudflare integration; the original September 4 backup is still the immutable reference. Production recovery and historical migration are **not enabled** by this checkpoint.

## Recovery preview and reviewed-migration rehearsal checkpoint

- SAVE TRACE now contains **Compare backup with current career**, a zero-write recovery preview. It parses a selected user-held JSON file locally in the browser, reads the **freshest** active IndexedDB record in a completed readonly transaction, validates backup/career/season/roster identity and revision, reports when the saved backup is older than current progress, warns if runtime has unsaved newer dates, and computes a SHA-256 file fingerprint when Web Crypto is available. There is **no restore/overwrite button**. The Download career backup action was tightened to re-read the latest saved record and use its matching active career identifier, never the stale panel-opening snapshot.
- The browser preflight's **actual source function** is extracted and tested in `tools/potential-v2-browser-recovery-preview.test.js`, including missing revision, wrong career, older/newer snapshots, differences in OVR/season and read-only behavior. This is not a live Safari acceptance test; user may test the new comparison button after the Cloudflare deployment. Their previously verified September 4 backup remains separate in iPhone Files.
- An independent **offline-only migration rehearsal** (`tools/potential-v2-migration-rehearsal.js`) clones an exported world and requires a reviewed, explicit per-player plan matching the original career identifier, date, revision and BOTH conflicting potential values. It forbids real prospects, duplicate IDs, stale ratings and unreviewed evidence. It synchronizes selected generated players' root and nested potential fields (plus the career root snapshot), appends—not replaces—their development history and assigns Medium confidence **only in the disposable clone**. Tests verify unrelated statistics, XP, upgrade counts, season/calendar, external real prospects and the original backup remain unchanged.
- The rehearsal never creates or authorizes a live migration; no candidate ratings are preapproved, including Alacai's unresolved 74 versus 68. A genuine fully tested user-confirmed production restore and a reviewed player-by-player potential proposal remain mandatory before any active career writes.
- Re-ran **10 offline/source-extracted regression suites** against the latest GitHub files; all passed in the JavaScript test harness. The new browser recovery code also passed a JavaScript syntax check. These results do **not** substitute for iPhone acceptance or a complete game boot from a restored backup.

## Exact original-versus-new backup diagnosis (September 24, 2026)

- The user uploaded their second 56 MB backup; the original and newest JSON were inspected **locally and read-only**, never committed. Both have the same active career, 2025-09-04 in-game date, 160 players across eight HS rosters, 191 external prospects, one Alacai career-player marker, 72 OVR and unchanged root/development potentials **74/68**. Whole saved teams, career player/root snapshots, external prospects, schedules, historical player statistics, attributes, development/earned XP and core world history matched exactly.
- **Confirmed root cause of the blocked recovery comparison:** the previous `world.js` `createDefaultSeasonState()` replaced `world.season` with a whitelist and **dropped `seasonId`, `seasonLabel`, `currentYear`, `careerYearIndex` and `schoolYear`** during Continue Career. The new backup still has correct `world.season.id = hs-2025-2026`, label `2025-26`, season number 3 and saved player school year Junior, so those aliases can be reconstructed without fabricating gameplay. The browser preflight was incorrectly hard-requiring the redundant `season.seasonId` alias.
- The original save's revision was **4**, but the later backup's revision was **1** despite an otherwise identical gameplay state and a later timestamp. The authoritative load had left `_saveRevision` at zero rather than recovering `storedRecord.revision`, so the first save after reload silently reset revisions. The latest `world.js` commit now resumes the revision counter from the exact persisted record, and preserves/reconstructs season aliases on load.
- The browser recovery preview and isolated verification now accept `season.id` as a canonical fallback; if a later saved timestamp has a lower revision, preview reports `REVISION_RESET_DETECTED` and tells the user to keep BOTH exports. The offline restore guard shares the canonical-ID fallback. The prior reset from 4 to 1 remains historical and cannot be retroactively undone by a read-only check.
- An **extracted production-source canonical-season test** covers original/missing-alias fixtures, idempotent repeated loads, root player preservation and revision-resume guard; the browser recovery source tests verify exact legacy alias and revision-reset handling. Neither a full gameplay boot nor a production restore has been claimed. Both originals are retained privately; no active save, player rating or XP was touched.
- Next iPhone action: after Cloudflare deploy, refresh, open SAVE TRACE, choose **Compare backup with current career**, select the user's original September 4 JSON and send the result. A `REVISION_RESET_DETECTED` verdict is expected until the live revision has naturally advanced; no user-triggered restoration is necessary. If needed, export a fresh backup only after confirming a successful game save.

## Two real iPhone backups: persistence root cause verified (2026-09-24)

- Compared the user's original 56 MB JSON export (`2026-09-24T05:56:01.123Z`) against **Backup 2** (`2026-09-24T22:29:06.371Z`) read-only and recursively. Exactly **10 JSON leaf differences**: two export/save timestamps, one regenerated postseason `rebuiltAt` timestamp, two revision values `4 → 1`, and **five silently discarded season aliases** (`seasonId`, `seasonLabel`, `currentYear`, `careerYearIndex`, `schoolYear`). All other saved values match exactly, including 160 roster players, 191 external prospects, the one marked career player, game date 2025-09-04, 72 OVR, Alacai's unresolved 74/68 potentials, historical records, attributes and XP. The canonical `season.id` remains `hs-2025-2026` in BOTH.
- Root causes in the original `world.js`: ordinary career load rebuilt `season` from a whitelist omitting existing aliases; the world save counter restarted from zero on page reload, so a later export recorded revision 1 rather than 4. **Neither the backup nor all five aliases should be rewritten as a live migration** merely to run this diagnostic.
- Fix committed in `world.js` (`870f76c`): preserve existing season metadata when normalizing; fill missing aliases from canonical season/player information without advancing the calendar; initialize the in-memory save revision from the authoritative IndexedDB record before any auto-repair save. The SAVE TRACE recovery preview separately accepts canonical `season.id` as a safe fallback and explicitly flags a later export with a lower revision as `REVISION_RESET_DETECTED` rather than treating it as newer gameplay.
- New direct-source regression suite `tools/potential-v2-season-persistence.test.js` (`10ee40e`) passes synthetic tests for original metadata retention, old-backup alias repair, two consecutive reloads, history/phase preservation, monotonic revision continuation and startup repair ordering.
- **iPhone recovery-comparison acceptance observed**: the original September 4 export (revision 4) versus the present live save (revision 2), both dated 2025-09-04, both season `hs-2025-2026`, both Alacai 72 OVR, returned the expected `REVISION_RESET_DETECTED` without altering the live career. The correct warning preserves BOTH exports. This confirms the **read-only comparison UI**, not that a new gameplay save after deployment or a full production restore has been tested. The next ordinary save from the fixed load flow should increment from its loaded live revision (2→3), not from zero; do not force extra simulations merely to advance a counter. Do not deploy historical potential migrations before a controlled restore and reviewed recalibration.

## Full-backup fidelity and reset-aware recovery guard (2026-09-24)

- The iPhone's **isolated restore already passed** its original selected-field read-back and disposable database cleanup. The disposable test now also traverses **every JSON field** of the entire restored IndexedDB career record, with exact array lengths and complete key/value matching. The check covers all attributes, earned/spent XP, archived statistics, timeline, season metadata, real prospects, scouting state and revisions—not just the earlier 11 spot checks. It uses iterative traversal and yields every 25,000 examined values to keep iPhone Safari responsive. A discrepancy reports the exact field path and fails the test; the separate live database is never opened for writes.
- New direct-source regression suite `tools/potential-v2-full-backup-fidelity.test.js` passes synthetic whole-record, single-XP mismatch, historical-stat mutation, missing season alias, external-roster change and large (>26,000 entries) checks. **Verified on iPhone on September 24, 2026:** user submitted screenshot showing PASS for isolated IndexedDB restore, full-record read-back and disposable database cleanup on original 56 MB career backup, with **3,155,706 saved values inspected**, 160 roster players, 191 external prospects, Alacai 72 OVR and game date 2025-09-04. Live career untouched. This closes the full-record disposable-fidelity gate, NOT the separate production-restore/full-game-boot gate.
- Updated `tools/potential-v2-restore-guard.js` to identify **later saves with historically reset lower revisions** via `savedAt` timestamps plus matching/advancing dates. In the observed real scenario (older export rev 4; later live rev 2; identical 2025-09-04 gameplay date), the offline guard now treats the live record as progress that must be protected. Its synthetic regression tests (22 passing assertions) include this exact reset chronology. Neither the browser preview nor this offline guard authorizes a production restore.
- Next strict gate: demonstrate full-record disposable fidelity on Safari, then develop an explicitly confirmed production recovery flow with a fresh current-save rollback copy, matching career identity and gameplay-boot verification before any live historical potential updates. **Do not treat the lower revision as permission to overwrite the later live save.**

## Offline atomic recovery and boot rehearsal (2026-09-24)

- **Full-backup device gate CLOSED:** user's iPhone confirmed isolated restore + full-field read-back of **3,155,706 saved values** + disposable IndexedDB deletion PASS, with no live-career writes. The original 56 MB JSON remains the immutable recovery source; retain the later backup separately.
- New `tools/potential-v2-recovery-rehearsal.js` is **not imported by the game** and has no connection to the browser's live IndexedDB. Its injected adapter is an isolated copy-on-write fake store. It refuses to plan recovery without an exact matching career/player identity, valid world, full-record verification, a separately verified rollback export and an explicit approval flag. An older in-game date is refused by default; root/nested potential, saved XP and all real-prospect records are preserved from the selected backup. A candidate restore receives a new revision above both the original and current revision, preventing the historical revision-reset bug from recurring on a recovery commit.
- Recovery is rehearsed as a **single atomic transaction** containing (a) the current full career rollback snapshot, (b) the candidate restore and (c) a pending-boot journal. Re-check the full live baseline inside the transaction to detect concurrent saves; verify all three records by read-back. Extract and run the real `world.js` season canonicalizer against the candidate in the test fixture. If boot rejects the candidate, the offline simulator uses another compare-and-swap transaction to restore the rollback record and refuses to destroy any gameplay changes made after candidate staging. Leave the rollback journal in place for later checks.
- Synthetic failure-injection tests: transaction rejection before any writes, failure after staging but before commit, lost acknowledgment **after** successful commit, missing rollback/boot readiness approvals, wrong player, older in-game backup, interrupted boot, failed boot, two competing recovery attempts, stale save before transaction and a newer save at rollback time. The lost-acknowledgment case first rereads the candidate/rollback/journal to determine whether the transaction actually committed rather than falsely claiming abort.
- `tools/potential-v2-recovery-rehearsal.test.js` passed **41 assertions** using the current production season normalizer and realistic fixture with saved potential conflict (74/68), XP and external real prospects. This proves the **offline protocol logic**, **not** the actual Safari IndexedDB multi-record atomic transaction, browser quota sufficiency, full-game boot or rollback after reloading the deployed app.
- **Next strict gate:** implement and test the protocol using a deliberately named **disposable IndexedDB database** and an isolated app-boot harness; measure capacity and handle QuotaExceededError atomically. Then expose an explicitly user-confirmed production recovery process only after safe rollback has been verified end to end. No production restore or Potential 2.0 migration was enabled at this checkpoint.

## Disposable IndexedDB atomicity test staged for iPhone (2026-09-24)

- Added a new **one-tap, file-free** button inside SAVE TRACE: `Test isolated recovery protocol`. Unlike the original 56 MB full-backup test, this uses **synthetic fixture data only** in a uniquely named disposable IndexedDB database: 8 fake Varsity HS teams, 160 generated roster entries, 191 fake real/external prospects, two archived seasons and an intentionally conflicting 74/68 fake career potential. It NEVER writes to `projectice_database`, localStorage, the real career record or the user's downloaded backup.
- Runs actual IndexedDB transaction stages in the browser: seed a fake live record; intentionally abort a multi-record write and confirm **all** attempted writes vanished; in **one real transaction** stage the candidate career, complete rollback copy and pending-boot journal; read all back; validate synthetic boot fields; intentionally reject boot and restore the original using compare-and-swap; then stage again, simulate a newer save and verify stale rollback **fails closed** instead of overwriting that newer progress. Finally close/delete the disposable database and separately report deletion status.
- Added `tools/potential-v2-fake-indexeddb.js` and `tools/potential-v2-disposable-protocol.test.js`. The **exact browser function** extracted from SAVE TRACE passed a transactional Node emulator test covering successful abort, atomic stage/read-back, rollback, newer-progress CAS, temporary-db cleanup and simulated quota failure. The emulator leaves a separate sentinel `projectice_database` record unmodified even when an injected quota exception rejects the test. This confirms offline control-flow behavior, **not yet real WebKit transaction handling or a complete game boot**.
- Keep the earlier **3,155,706-field original 56 MB backup verification PASS** distinct from this smaller protocol test. The next device checkpoint is to run the new isolated protocol button in iPhone Safari after deployment and report PASS/NOT VERIFIED. Only after that can we evaluate a separately approved production recovery process with a fresh rollback export and a full-game boot test. No live potential/XP recalibration is enabled.

## iPhone disposable atomic-recovery protocol accepted (September 24, 2026)

- User supplied the actual iPhone SAVE TRACE result: **PASS** for every check in the new synthetic disposable-database protocol: intentional multi-record transaction abort, candidate/rollback/journal atomic stage, full staged-record read-back, rollback after simulated failed boot, rejection of stale rollback after newer progress, and successful disposal of the temporary database. Neither the real career nor the user's JSON backup was changed. This closes the **Safari disposable IndexedDB transaction and cleanup checkpoint**, not production recovery or full-game boot.
- The separate real-data isolated backup verification remains **PASS across 3,155,706 saved values** from the original 56 MB JSON. Keep both private backups. Next engineering work can proceed without additional user actions: validate actual full-world boot from an imported snapshot in a strictly isolated runtime, then design a production restore with a verified fresh rollback export, explicit approval, atomic journaling, compare-and-swap rollback and recovery after interrupted boot. Do not add a live restore control or mutate current career until those additional gates have passed and the user authorizes the action.

## Full-game boot isolation requirement

- Audited the actual `world.js` startup path after the iPhone disposable recovery PASS. Unlike the pure season canonicalizer used by our previous synthetic recovery tests, normal `WorldEngine.load()` is **not a read-only loader**: it can delete the obsolete default record, migrate an old single-world save, repair award news, normalize player contracts, and trigger automatic follow-up saves. Therefore, placing an imported candidate into the existing active browser database merely to see if gameplay starts is **not an acceptable boot test**.
- The next boot harness must use a completely separate origin/storage environment **or** a deliberately injected disposable persistence adapter that intercepts every IndexedDB and localStorage operation, with no ability to resolve to `projectice_database` or to the active-career storage keys. Assert no writes to real storage, then verify the imported world's calendar, school year, 160 roster slots, real external prospects, Alacai identity/OVR, preserved XP/history, standings, upcoming schedule and resumed simulation initialization. A tested rollback journal and a fresh live export remain separate prerequisites for an eventual user-approved production restore.

## Actual WorldEngine isolated boot and reload (2026-09-24)

- New `tools/potential-v2-isolated-world-boot.test.js` **executes the unmodified production `public/world.js`** inside a Node VM; only its IndexedDB, localStorage, browser globals and console are replaced with isolated in-memory dependencies. It also loads the actual current `public/prospects.js` catalog rather than inventing the 191 external prospects. The fixture itself has a **synthetic** Varsity world with eight HS teams and 160 roster slots, two historical seasons, the intentionally unresolved 74 root / 68 development potential, 62 speed XP, Junior school year and September 4, 2025 preseason calendar.
- The source-extracted test **PASSED**: `WorldEngine.load()` hydrated the exact synthetic career; a SECOND independent WorldEngine instance loaded the SAME disposable record without advancing the season, changing XP, losing player history or altering any persistent record; a deliberate `WorldEngine.save()` **only inside the sandbox** advanced the persisted revision from 4 to 5, retained 160 roster slots and all 191 curated external real prospects, and left an unrelated fake-career sentinel completely unchanged. The test also checks the active career identity cannot be switched by an ordinary load.
- Extended `tools/potential-v2-fake-indexeddb.js` with full `getAll()` support to exercise the production load's **actual** multi-career authority checks. The VM has **no real browser IndexedDB or localStorage access**; all localStorage traces and possible repair writes are captured within the fake adapter. This is a stronger test than calling the isolated season canonicalizer alone.
- **Scope limit:** this passed test does NOT use the user's actual private 56 MB backup, does not execute `public/game.js` or its DOM/UI continuation, and does not constitute a full deployed Cloudflare/iPhone gameplay-boot or production restore. Using the current curated real-prospect catalog is essential: `ensureExternalProspectWorld()` can intentionally drop stored real-prospect IDs no longer present in the curated current source. A future real-snapshot isolated boot must report any such legitimate catalog changes distinctly from accidental data loss, before any recovered career is accepted.
- Next: build a **private-file optional** version of this same sandbox test for the user's original exported JSON, compare its before/after whole-world invariants without writing the source file, then add the isolated game.js continuation harness. Keep production recovery and live potential migration disabled until a complete trusted rollback and iPhone app-boot procedure is demonstrated.

## Isolated production WorldEngine boot and atomic recovery integration (2026-09-24)

- After the confirmed iPhone disposable transaction PASS, added and tested `tools/potential-v2-isolated-world-boot.test.js` and `tools/potential-v2-staged-production-boot.test.js`. These use the **actual current production `public/world.js` and `public/prospects.js`** as source, instantiated with injected, private fake IndexedDB and fake localStorage. WorldEngine's usual `projectice_database` open request is strictly **remapped to a uniquely named disposable test database**. A separate fake `projectice_database` contains a canary career that must remain bit-for-bit untouched.
- Isolated direct boot PASS: 160 synthetic Varsity roster slots across 8 teams, all 191 **actual curated prospect definitions** from prospects.js, two archived high-school seasons, current calendar 2025-09-04, Junior year, Alacai-like intentional potential conflict (root 74/development 68), preserved saved 62 speed XP and original schedule. The actual `WorldEngine.load()` succeeded **twice**, public career/team lookup APIs resolved correctly, an ordinary sandbox `WorldEngine.save()` advanced revision **4→5**, and a missing active career correctly refused to load rather than resurrect another saved career. All live-store canaries remained unchanged.
- Newly integrated recovery-boot test PASS: in one isolated atomic IndexedDB-style transaction stage the complete original synthetic career as rollback plus a higher-revision recovery candidate and pending-boot journal. Call the **real `WorldEngine.load()`** on that staged candidate, then simulate downstream UI boot failure and atomically rollback to the original record. Confirm the rolled-back original boots; restage and boot the candidate again, make an actual sandbox game save (revision **5→6**, date 2025-09-05), then show that a delayed stale rollback **aborts instead of overwriting newer progress**. The original input records, saved XP, historical stats, roster, external prospects and separate fake real-user canary all remain unchanged.
- Both regression tests passed against GitHub `main` source using the injected transactional JS emulator. These results establish **offline core WorldEngine hydration and save/recovery orchestration**, not browser WebKit execution of the actual full-game UI, not a real 56 MB file imported into full gameplay, and not a live production restore. This division matters because the actual load function can delete a legacy default record, normalize scouting and repair news: it was exercised ONLY against injected isolated storage.
- **Next gate:** validate the real 56 MB backup's full-state game/UI initialization in an isolated environment, including timeline, current schedule and interactive game state, before implementing any user-confirmed production restore. Keep both original backups. All live potential/XP migration remains disabled.

## Optional private-backup world boot and actual backup preflight (2026-09-24)

- Added `tools/potential-v2-private-world-boot.js`: a **local-only** offline CLI that accepts a separately supplied private JSON backup file path; the source file is never overwritten, committed, uploaded or altered. Its full production `world.js` and curated `prospects.js` run inside an isolated VM with *injected fake* IndexedDB/localStorage and an unrelated live-database canary; the VM remaps all production database opens to the disposable fake database. It verifies the exact backup envelope and player identity; date, season, career year, school year, 160 roster slots, current potential values, XP, archived seasons, team/player lookup APIs, two successive boots, preserved source-file SHA-256 and absence of real datastore writes. It detects and reports any startup repairs/auto-save and catalog-related external-prospect count differences rather than silently assuming every loaded state stays identical.
- Added `tools/potential-v2-private-world-boot.test.js` with a **synthetic-only** local test fixture generated from actual `world.js` defaults and current curated `prospects.js`. Regression tests PASS for 160 roster slots, 191 real-prospect definitions, two consecutive actual WorldEngine boots, the saved 74/68 potential conflict, original file byte preservation, correct revision, rejected incorrect career ID and rejected malformed season, with all storage isolated. Do **not** put the 56 MB user export in GitHub or the regression fixtures.
- The original user's mounted private 56 MB JSON was also examined **read-only** in the offline environment: SHA-256 `397aefb65e24db4b0d36bea6d2b6481898c4f2ae78c5c25f215fa2fc19f2f25f`; valid active-career envelope, 160 uniquely identified roster players and exactly one career marker, 191 external prospects, Alacai 72 OVR and root/nested potentials 74/68, 22 attribute-XP keys, two archived seasons, matching Junior/2025-09-04 season aliases, and 364 saved schedule entries (361 unfinished from September 4 onward). This was a **schema/schedule preflight, not execution of the new Node private boot tool on that backup**.
- **Current strict gate:** execute the private boot CLI against the real 56 MB backup in an isolated environment that has the current production source files, and then test actual `game.js` UI initialization against that isolated snapshot. Neither the synthetic VM PASS nor iPhone's full-field disposable IndexedDB PASS establishes that a complete real user career boots and resumes through every game UI. Do not enable production recovery or live potential migration without that check and explicit user approval.

## Game.js startup fail-closed guard and private boot continuation (September 24, 2026)

- Resumed the interrupted isolated-boot work. The offline private-backup loader, actual `WorldEngine` VM double-boot fixture and staged recovery + rollback integration test are already committed under `tools/potential-v2-private-world-boot.js`, `tools/potential-v2-isolated-world-boot.test.js` and `tools/potential-v2-staged-production-boot.test.js`. The latest spec retains the **real original 56 MB backup's read-only schema/schedule preflight** and explicitly distinguishes it from a fully executed private-source world/game UI boot.
- Source audit of the **real `public/game.js` startup** found a new safety issue: `init()` previously ignored a failed `WorldEngine.load()` and always ran `WorldEngine.ensureGeneratedRosters()`. With an active career ID but an unavailable IndexedDB record, this risked bootstrapping substitute rosters rather than failing closed. Corrected the app entry point: when an existing active career does not load, show the title/Continue UI for diagnosis and **do not generate rosters or recover a false preview**; an actual new career with no active ID still uses the original initialization path.
- Added source-extracted `tools/potential-v2-game-startup-guard.test.js` (three PASS scenarios): healthy saved career, temporarily unavailable existing career, and first-ever new career. The production app JavaScript syntax check also passed. This is a narrow runtime safety fix; no ratings, XP, roster or persisted career records were migrated.
- The previous private loader and synthetic staged-boot tests do NOT establish that the user's entire real 56 MB snapshot successfully runs `game.js` UI. Next gate remains an actual isolated original-backup WorldEngine/game integration run with no access to real storage. Browser acceptance and a user-approved production restore are separate later gates.

## Implementation gates

1. **Backup & isolated restore:** retain original JSON on user phone. Verify full envelope and that a disposable IndexedDB database can store and round-trip a 56 MB snapshot without affecting active storage. iPhone disposable restore has passed; next verify controlled full-game restoration separately before any mass migration. Do not offer destructive restore without explicit confirmation and a newer-live-save warning.
2. **Read-only historical scorer:** compute position-, age- and competition-normalized evidence from available archived records. Output per-player proposed rating, role, confidence and trend, plus evidence strength, gaps and reasons for no change. No IndexedDB writes. Include career player and all eligible generated HS NPCs; exclude real-player records.
3. **Review and calibration:** simulate elite teenage breakout, moderate growth, aging NHL Elite production, two years underperformance, goalie exceptional season, overlooked late bloomer, cap-85 HS case, limited exposure and zero-stat case. Review draft-class statistical distribution separately. Review historical proposed changes before write enablement.
4. **One evaluator / XP integration:** implement canonical weekly potential engine and reconnect XP upgrade-cost function to canonical potential and earned breakout evidence. Test both manual career-player upgrades and NPC automatic upgrades. Ensure one weekly pass is idempotent and deterministic per season, player and date.
5. **Display compatibility:** Home, Player, scouting report and recap consume canonical projections; hidden personality remains hidden. Existing saved histories remain visible.
6. **Controlled migration:** on explicit approval, take an additional current backup, compare against original baseline/date, stage all proposed writes in a copy, verify no changes to XP/OVR/stats/season timeline or external prospects, then commit atomically once. Mark migration version and ensure a second load does not reapply.
7. **iPhone acceptance:** reload twice and check current date, player ratings, certainty/trend, upgrade prices, scouting ranks, past season stats, Rival Watch, timeline and event simulation before resuming play.

**Stop condition:** if backup restoration or any invariant fails, do not modify the live save. Continue new-engine work in isolated fixtures until repaired.

## Real original-backup verification checkpoint (continued)

- Re-ran the committed private-world boot CLI against the original, separately provided 58,201,560-byte September 4 backup. **PASS**: SHA-256 `397aefb65e24db4b0d36bea6d2b6481898c4f2ae78c5c25f215fa2fc19f2f25f`; two real production WorldEngine VM boots both retained revision 4, 160 roster slots, 191 external prospects, 364 schedule events and the current September 4, 2025 career date. No boot auto-save was detected by that isolated world-only harness. Neither the source backup nor the separate fake production-store canary changed.
- Re-ran the committed source-extracted `potential-v2-game-startup-guard.test.js`: three startup scenarios **PASS**; production `public/game.js` syntax check **PASS**.
- Investigated the previously observed disposable revision **4→5** when `game.js init()` executes. The current `init()` calls `await WorldEngine.ensureGeneratedRosters()` after successful load, and that production function ends with an **unconditional** `return WorldEngine.save()`. Therefore the revision increment on normal game init is explained by the existing startup save path, whereas direct `WorldEngine.load()` alone retains revision 4. A revision increment by itself is **not** proof of corrupted gameplay data. Do not remove this save without testing the roster/deployment migrations it persists.
- Attempted the next real-DOM, private-backup browser integration with locally bound isolated HTTP and headless Chromium. Browser navigation to localhost, local file URLs, and a route-intercepted test HTTPS hostname all returned `ERR_BLOCKED_BY_ADMINISTRATOR` in this execution environment. **No complete browser DOM/game UI boot can be claimed from this checkpoint.** No career backup was included in source control or hosted on Cloudflare.
- Next gate: validate actual game UI startup/Continue render against the private original backup in an authorized isolated browser environment, or use an on-device disposable-domain/storage diagnostic with explicit no-live-write protections. Continue to keep recovery and Potential 2.0 migration disabled pending browser acceptance and explicit user approval.

## Chromium real-DOM synthetic career smoke test (September 25, 2026)

- The previous complete-browser attempt using the actual 56 MB private backup did **not** finish: moving the entire huge original record into the browser-backed in-memory fake IndexedDB timed out in this environment. A smaller real-data projection also timed out. Neither is a successful full original-backup browser acceptance, and the original private file must remain off GitHub.
- A new **synthetic-only** Chromium smoke test executed the unmodified production `prospects.js`, `world.js`, and `game.js` against the actual `index.html` DOM at mobile viewport (390 × 844). An injected disposable in-memory IndexedDB adapter intercepted every production DB open; a synthetic canonical career, synthetic localStorage and an unrelated protected database canary were seeded before calling actual `init()`. This isolated smoke test is not real WebKit IndexedDB or an iPhone deployment test.
- The **actual screen click sequence PASSED**: title screen showed enabled Continue; clicking Continue opened the Career Saves screen with **1 Save**; clicking its actual save card loaded the **Career Hub** (visible `hub-screen`, `Game.screen === 'hub'`), retained all **160 synthetic roster slots**, and left the separate canary revision at **444**. There were zero browser JavaScript page errors or console warnings. The test exercises the actual `renderCareerSaveSelection()` and `selectCareerSave()` path, unlike the earlier source-extracted handler test.
- Synthetic fixture's 72 OVR was normalized to 73 by production initialization (fixture uses deliberately incomplete attributes); do **not** extrapolate this rating change to the original full career. The original backup's 72 OVR was preserved in earlier private WorldEngine and extracted game-handler tests.
- Synthetic disposable revision progressed 4→5 on init and 5→9 after selecting the save. The previous investigation identified the unconditional `ensureGeneratedRosters()` save on normal startup; further UI/save sequencing can legitimately save additional times. Do not treat a higher revision alone as a gameplay corruption indicator.
- Still missing: a full original-backup **real-DOM** startup, an authorized iPhone Safari / Cloudflare end-to-end check, verified fresh live rollback export, and explicit user-approved production recovery. Do not enable live restore or Potential 2.0 migration.

## Standalone harness execution fix and renewed acceptance (September 25, 2026)

- **Discovered and fixed a real regression-harness execution bug:** `artifacts/project-ice/package.json` declares `"type":"module"`, but the checked-in `tools/*.js` regression harnesses use CommonJS `require()`. Executing `node tools/potential-v2-private-world-boot.js ...` directly from a clean repository checkout failed with `ReferenceError: require is not defined in ES module scope`. Commit `168893aa` added narrowly scoped `tools/package.json` declaring `"type":"commonjs"`; production app bundling and public gameplay scripts are unchanged.
- After adding that local package scope, re-executed the **real original 58,201,560-byte private backup** in the existing standalone `WorldEngine` harness: **PASS**, source SHA-256 `397aefb65e24db4b0d36bea6d2b6481898c4f2ae78c5c25f215fa2fc19f2f25f` unchanged, both isolated boots revision **4**, OVR **72**, **160** roster players, **191** external prospects, **364** schedule entries, no warnings or WorldEngine boot auto-save; the separate original-name database canary stayed untouched.
- Executed synthetic private-boot regression with double WorldEngine boot, wrong-career rejection and input immutability **PASS**; source-extracted three-scenario game.js startup guard **PASS**; browser-recovery preview, disposable atomic/CAS protocol, 15 restore-preflight assertions **PASS**; `node --check public/game.js` **PASS**.
- Repeated a **real Chromium mobile-DOM synthetic test locally** using the repository's actual HTML plus production prospects/world/game scripts and a disposable browser in-memory IndexedDB adapter: actual title Continue enabled → actual Career Saves UI displayed exactly one save → click opened Career Hub, all 160 synthetic roster slots present. No claim of real iPhone/WebKit persistence is made. Commit `94ded64` corrected escaping in the checked-in browser harness's HTML external-script removal regular expression. Chromium run above was from a matching minimal local test driver, not the checked-in driver itself.
- Retried injecting the complete **private original backup** into the same real-browser fake IndexedDB UI test; the job **timed out before a usable result**. Do not mark full original-backup browser acceptance complete. The original file remained private and unchanged; no live browser or Cloudflare restore ran.
- Remaining gate: full original-data UI flow under an authorized browser execution budget (consider efficient file-backed/streaming fixture transfer), real on-device Safari smoke test in isolated storage, fresh verified rollback export, and explicit approval before any live restore or potential migration.


## Full original private career browser UI startup PASS (September 25, 2026)

- **Major gate cleared:** The exact original, separately provided **58,201,560-byte** backup (SHA-256 `397aefb65e24db4b0d36bea6d2b6481898c4f2ae78c5c25f215fa2fc19f2f25f`) completed an actual **Chromium mobile-viewport (390×844) DOM startup + Continue screen + saved-career selection + Career Hub render**, using unmodified production `public/prospects.js`, `public/world.js`, `public/game.js`, and actual `index.html`. Result **PASS**; no page errors or console warnings.
- The earlier 56 MB browser-transfer timeout was avoided by gzip-compressing the exact private JSON on the local test runner (~5.76 MB compressed) and streaming it into the isolated browser with `DecompressionStream`. The browser's **test-only** fake IndexedDB adapter used identity cloning and skipped test-adapter quota byte counting to eliminate unrelated repeated full-record JSON serialization. This is deliberately NOT a claim about real browser IndexedDB memory or quota behavior.
- On actual Continue click, the real DOM displayed **Career Saves: 1 Save** with the original player's career; clicking the actual save card displayed **Career Hub**. Post-Continue invariants: 72 OVR, root potential **74**, development potential **68** (not silently reconciled), **160** roster slots, **191** external prospects, **364** schedule entries, `2025-09-04` world date, **22** attribute XP keys. Disposable save revision changed 4→9 via current production startup/selection code; the separate protected-name database canary remained revision **444**. All 10 database opens were mapped exclusively to a uniquely named disposable test database; **zero deletes**; source backup SHA-256 unchanged.
- Reproducible private-only generic harness added at `tools/potential-v2-private-browser-boot.py` (commit `6f5d377`). It accepts the local backup pathname as CLI argument. **No user backup, career record or personal save data has been committed.**
- Scope: **production DOM/rendering and actual game handlers exercised**, but **Chromium with a lightweight fake identity-clone IndexedDB**, not iOS WebKit or persistent browser IndexedDB. Existing separate full atomic commit/rollback tests still apply; combined test does not yet certify full-sized persistent IndexedDB writes or Cloudflare/iPhone end-to-end. **Do not migrate potential or restore into the live career** until a fresh rollback backup and on-device acceptance are in place and the user explicitly approves.
- Next candidate: strengthen production browser storage acceptance under real IndexedDB in a separate disposable origin, check iPhone Safari/Cloudflare startup against an isolated save, or investigate the normal revision 4→9 writes to ensure state diff excludes unexpected potential/XP/roster changes.


## Original-backup startup save-diff audit and native browser storage boundary (September 25, 2026)

- Ran full original 58,201,560-byte private backup through actual Chromium mobile-DOM `init()`, title Continue, Career Saves selection, and Career Hub. Original SHA-256 remained unchanged and no console/browser errors were observed. **PASS**.
- Added a before/after **persisted disposable save** projection over world keys/date/week/season, 160-player roster identities/overall/root and development potentials/XP counters/attribute XP, 191 external prospect ratings/potential, 364 schedule entries, and original player's potential/attribute XP/upgrades/season history. On actual Continue-to-Hub load, the **only observed projection changes were `revision: 4→9` and `savedAt` timestamp**. No projected gameplay, roster, prospect, XP, potential, or schedule field changed. This explains normal repeated save writes without evidence of an unintended recovery overwrite. The projection is intentionally scoped; it is **not** a byte-for-byte deep diff of all 56 MB or a genuine-browser IndexedDB durability check.
- Commit `0d6b38f` adds these projected no-mutation assertions to the reusable **private-only** browser acceptance harness. It contains no original career data.
- Attempted a separate native Chromium IndexedDB acceptance: `about:blank` / Playwright `set_content()` has opaque origin, and native `indexedDB.open()` throws **SecurityError** there; loopback HTTP navigation remains blocked by `net::ERR_BLOCKED_BY_ADMINISTRATOR`, even with Chromium `--no-proxy-server`. This execution environment therefore cannot certify real persistent browser IndexedDB or real Safari. The fake adapter's transactional rollback unit tests remain valuable but distinct.
- The original backup and protected-name database canary were unchanged; no live restore or Potential 2.0 migration ran. Next: verify a real-origin native IndexedDB workflow in an allowed browser environment, then iPhone Safari Cloudflare acceptance using a disposable account/storage origin, fresh backup/rollback verification and user approval before production migration.


## Exhaustive original 56 MB save-state diff (September 25, 2026)

- Advanced from sampled/projection-only comparisons to an **exhaustive, every-field JSON comparison of the entire original private 58,201,560-byte career world** in the same isolated production Chromium mobile-DOM `init() → Continue → actual Save selection → Career Hub` flow. Exact before/after serialized world lengths both **58,198,505 UTF-16 code units**. Iterative deep walk across the full graph found **exactly one difference**: `world.persistence.revision: 4 → 9`, matching normal save metadata. Every other JSON world value and field, including unprojected history, rosters, player XP and potential, scouting, schedule, simulation state, and news, matched. The envelope's `revision` and `savedAt` also changed as normal metadata. All assertions **PASS**; backup SHA-256 unchanged, original career 72 OVR, protected canary untouched.
- This test uses the same **identity-copy fake IndexedDB** and production unmodified UI scripts. It is strong application-level no-unintended-mutation evidence, **not** genuine Chromium IndexedDB durability, Safari/WebKit quota, or production Cloudflare acceptance. The original full career remains private and was not committed.
- Commit `81d5397` makes the strict exhaustive check reproducible in `tools/potential-v2-private-browser-boot.py`. The check intentionally fails if anything other than the expected inner metadata revision changes; if startup/save semantics are deliberately updated later, refresh documented assertions only after confirming the full diff.
- Genuine persistent browser IndexedDB and isolated iPhone Safari acceptance remain the two critical release gates; do not restore/migrate live career until rollback is freshly verified and the user approves.
